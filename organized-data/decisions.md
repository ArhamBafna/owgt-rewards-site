# Decision Log

- Created `organized-data/` folder as a copy of `data/`.
- Created `Tools` category and extracted 9 tools from `notion.txt`.
- Removed `notion.txt` because extraction captured all meaningful knowledge.
- Initialized `master.md` and `master.json`.
- Created new categories: `Frameworks`, `Guides`, `Learning`, `Resources`. Created `README.md` for each explaining what belongs there.
- Extracted 19 knowledge objects from `github.txt` into appropriate categories.
- Ignored Ollama from `github.txt` as it was already properly documented in `Tools` from the first batch (merged conceptually without overriding the better homepage link).
- Removed `github.txt` because extraction captured all meaningful knowledge.
- Updated `master.md` and `master.json` to include the new objects.
