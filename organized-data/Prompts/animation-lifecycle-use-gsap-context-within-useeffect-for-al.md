### Name
### Animation Lifecycle
- Use `gsap.context()` within `useEffect` for ALL animations. Return `ctx.revert()` in the cleanup function.
- Default easing

### Description
A prompt for ### animation lifecycle
- use `gsap.context()` within `useeffect` for all animations. return `ctx.revert()` in the cleanup function.
- default easing tasks.

### Prompt
```text
`power3.out` for entrances, `power2.inOut` for morphs.
- Stagger value: `0.08` for text, `0.15` for cards/containers.
```

### Category
Thinking & Strategy

### Tags
- Thinking & Strategy
- Prompt
- ###
